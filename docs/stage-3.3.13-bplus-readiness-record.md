# Stage 3.3.13 B+ Readiness Record

本阶段执行方案 B+ 的文档记录部分：只记录客户端验收结果和 cutover readiness 状态。本阶段不是正式 cutover，不连接服务器，不执行远程命令，不触发后端任务，不修改正式节点链接。

## 当前阶段结论

- 已选择方案 B+。
- 本阶段只记录客户端验收结果和 cutover readiness 状态。
- 本阶段不是正式 cutover。
- `node.share_link` 不允许修改。
- `gost` 8443 继续保留为当前正式/回退链路。
- `socat` 18443 继续作为候选正式链路。
- 不切换端口。
- 不让 `socat` 接管 8443。
- 不关闭、停用或降级 `gost` 8443。
- B+ 完成后仍需新的人工 Go / No-Go 才能进入 C 方案。

## B+ 范围说明

方案 B+ 的范围仅限：

- 记录客户端真实验收结果。
- 记录 cutover readiness 状态。
- 记录人工审批结论。
- 保留当前链路关系。
- 为未来是否进入方案 C 提供依据。

方案 B+ 不包含：

- 不修改 `node.share_link`。
- 不写入候选链接为正式链接。
- 不新增数据库迁移。
- 不新增监听端口。
- 不执行远程命令。
- 不触发后端任务。
- 不让 `socat` 接管 8443。
- 不关闭、停用或降级 `gost` 8443。
- 不做正式 cutover。

## 人工审批结论表

| 审批项 | 结论 | 说明 |
| --- | --- | --- |
| 最终方案 | B+ | 只记录客户端验收结果和 readiness 状态 |
| 是否允许正式 cutover | 否 | 本阶段不得执行正式 cutover |
| 是否允许修改 `node.share_link` | 否 | 正式节点链接保持不变 |
| 是否允许远程命令 | 否 | 不执行 SSH 或任何远程命令 |
| 是否让 `socat` 接管 8443 | 否 | 18443 仍为候选链路 |
| 是否停用 `gost` 8443 | 否 | 继续保留为正式/回退链路 |
| 18443 客户端是否已真实测试 | 是 | 已覆盖三种客户端场景 |
| 测试时间 | 2026-06-07 | 按人工验收结论记录 |
| 测试结果 | 可用 | `socat` 18443 候选链路可用 |
| 是否影响直播 | 无影响 | 继续保留 `gost` 8443 回退链路 |

## 客户端验收记录表

| 测试时间 | 测试客户端 / 场景 | 候选链路 | 测试结果 | 是否影响直播 | 回退链路状态 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-06-07 | Shadowrocket | `socat` 18443 候选正式链路 | 可用 | 无影响 | `gost` 8443 保留 | 不记录完整节点链接 |
| 2026-06-07 | v2rayN | `socat` 18443 候选正式链路 | 可用 | 无影响 | `gost` 8443 保留 | 不记录完整节点链接 |
| 2026-06-07 | 软路由 | `socat` 18443 候选正式链路 | 可用 | 无影响 | `gost` 8443 保留 | 不记录完整节点链接 |

验收记录不得写入完整 `vless://`、`vmess://`、`trojan://` 链接，不得写入 SSH Key、Passphrase、token、服务器密码或 Reality privateKey。

## Cutover readiness 状态表

| 项目 | 当前状态 | Readiness 结论 | 说明 |
| --- | --- | --- | --- |
| `gost` 8443 回退链路 | 保留 | Ready for fallback | 继续作为当前正式/回退链路 |
| `socat` 18443 候选链路 | 可用 | Candidate ready | 已通过 Shadowrocket、v2rayN、软路由测试 |
| `node.share_link` | 未修改 | Locked | 本阶段不允许修改 |
| 远程命令权限 | 不允许 | Blocked | 本阶段不得执行远程命令 |
| 数据库迁移 | 不允许新增 | Not needed | 本阶段只写文档记录 |
| 新增监听端口 | 不允许新增 | Not needed | 继续使用已验证的 18443 |
| 回滚方案 | 保留 `gost` 8443 | Ready for B+ | 如果候选链路异常，继续使用 `gost` 8443 |
| 操作窗口 | 未进入正式执行 | Not applicable | 本阶段不执行 cutover |
| 方案 C 准入 | 未批准 | No-Go | 需新的人工 Go / No-Go |

## node.share_link 修改限制

本阶段明确不允许修改 `node.share_link`。

如未来需要修改 `node.share_link`，必须单独进入正式 cutover 或批准阶段，并重新确认：

- 是否允许修改。
- 旧值是否已安全备份。
- 回滚方式是否已验证。
- 是否禁止把完整链接写入文档、日志或报告。
- 客户端是否已准备好快速回退。

## 远程命令限制

本阶段明确不允许执行远程命令。

不得执行：

- SSH 登录。
- systemd 操作。
- 防火墙或安全组修改。
- `iptables` / `nft` 操作。
- 端口切换。
- `gost` 停止、降级或替换。
- `socat` 接管 8443。

## 8443 接管限制

本阶段明确不允许让 `socat` 接管 8443。

如果未来进入方案 C 并涉及 8443 服务切换，必须重新确认：

- 云服务器安全组 / 云防火墙已放行 TCP 8443。
- 服务器防火墙已放行 TCP 8443。
- 当前监听 8443 的服务。
- 切换后监听 8443 的服务。
- `gost` 8443 回滚方式。
- 客户端快速回退方式。

## gost 8443 保留限制

本阶段明确不允许关闭、停用或降级 `gost` 8443。

`gost` 8443 继续保留为当前正式/回退链路。任何删除、停用、替换或降级都必须单独进入新的审批阶段。

## 进入 C 方案前的要求

B+ 完成后仍不得自动进入 C 方案。

进入 C 方案前必须重新获得人工 Go / No-Go，至少确认：

1. 是否允许正式 cutover。
2. 是否允许修改 `node.share_link`。
3. 是否允许远程命令。
4. 是否允许让 `socat` 接管 8443。
5. 是否允许停用、降级或替换 `gost` 8443。
6. 回滚方案是否已最终批准。
7. 操作窗口是否已确认。
8. 客户端回退方式是否已确认。

## 剩余阻塞项列表

当前仍存在以下阻塞项：

- 正式 cutover 未获批准。
- 修改 `node.share_link` 未获批准。
- 远程命令执行未获批准。
- `socat` 接管 8443 未获批准。
- 停用、降级或替换 `gost` 8443 未获批准。
- 方案 C 回滚执行清单尚未单独验收。
- 正式 cutover 操作窗口尚未确认。
- Docker compose 完整部署验收此前仍有本地环境限制记录。

以上阻塞项解除前，必须保持 No-Go。

## 安全边界声明

- 本阶段只写文档记录。
- 本阶段不修改代码。
- 本阶段不修改 `.env`。
- 本阶段不读取或修改私钥、服务器密码、真实数据库、真实日志。
- 本阶段不写入真实 SSH Key。
- 本阶段不写入 Passphrase。
- 本阶段不写入 token。
- 本阶段不写入完整节点链接。
- 本阶段不修改 `node.share_link`。
- 本阶段不新增数据库迁移。
- 本阶段不新增监听端口。
- 本阶段不执行远程命令。
- 本阶段不触发后端任务。
- 本阶段不关闭 `gost` 8443。
- 本阶段不让 `socat` 接管 `8443`。
- 本阶段不做正式 cutover。
