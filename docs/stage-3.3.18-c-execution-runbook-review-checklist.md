# Stage 3.3.18 C Execution Runbook Review Checklist

本阶段只新增 C 方案执行级 runbook 草案复核清单。本阶段不是正式 cutover，不连接服务器，不执行远程命令，不触发后端任务，不修改业务代码，不修改正式节点链接。

## 当前阶段结论

- 本阶段是 C 方案执行级 runbook 草案复核清单。
- 本阶段不是正式 cutover。
- 当前仍保持 No-Go。
- 本阶段只复核文档完整性，不执行任何步骤。
- 本阶段不修改 `node.share_link`。
- 本阶段不新增数据库迁移。
- 本阶段不新增监听端口。
- 本阶段不执行远程命令。
- 本阶段不触发后端任务。
- 本阶段不关闭、停用或降级 `gost` 8443。
- 本阶段不让 `socat` 接管 `8443`。

## Stage 3.3.17 草案摘要

Stage 3.3.17 C execution runbook draft 已包含：

- 执行前硬性前置条件。
- 端口与防火墙前置检查。
- Phase 0 到 Phase 7 的未来正式执行流程草案。
- 正式切换步骤草案。
- 验证步骤草案。
- 回滚步骤草案。
- 失败判定条件。
- 停止条件。
- 验收记录模板。
- 安全边界声明。

## Runbook 结构完整性复核表

| 复核项 | Stage 3.3.17 是否包含 | 复核结论 | 备注 |
| --- | --- | --- | --- |
| 前置条件是否完整 | 是 | 可复核 | 仍需未来执行前填写真实审批 |
| 执行流程是否完整 | 是 | 可复核 | Phase 0 到 Phase 7 已列出 |
| 验证步骤是否完整 | 是 | 可复核 | 覆盖客户端、网页、平台、直播、回退 |
| 回滚步骤是否完整 | 是 | 可复核 | 仍需未来形成可执行命令级 runbook |
| 失败判定是否完整 | 是 | 可复核 | 覆盖连接、平台、直播、8443、回退、敏感信息 |
| 停止条件是否完整 | 是 | 可复核 | 覆盖 Go、窗口、负责人、备份、授权、防火墙、回退 |
| 验收模板是否完整 | 是 | 可复核 | 不得包含敏感信息 |
| 敏感信息边界是否完整 | 是 | 可复核 | 禁止完整链接、密钥、token、密码 |
| 是否仍然保持 No-Go | 是 | 通过 | 本阶段不授权正式 cutover |

## 前置条件复核清单

| 检查项 | 是否在草案中明确 | 复核结论 |
| --- | --- | --- |
| 是否必须获得新的人工 Go | 是 | 通过 |
| 是否明确 `node.share_link` 修改权限 | 是 | 通过 |
| 是否明确远程命令权限 | 是 | 通过 |
| 是否明确 `socat` 接管 8443 权限 | 是 | 通过 |
| 是否明确 `gost` 8443 停用 / 降级 / 替换权限 | 是 | 通过 |
| 是否明确执行窗口 | 是 | 通过 |
| 是否明确执行人、验收人、回滚负责人 | 是 | 通过 |
| 是否明确客户端快速回退方式 | 是 | 通过 |
| 是否明确验收记录模板 | 是 | 通过 |
| 是否明确敏感信息禁止规则 | 是 | 通过 |

## 端口与防火墙复核清单

| 检查项 | 是否在草案中明确 | 复核结论 |
| --- | --- | --- |
| 云服务器安全组 TCP 8443 | 是 | 通过 |
| 云防火墙 TCP 8443 | 是 | 通过 |
| 服务器防火墙 TCP 8443 | 是 | 通过 |
| 当前监听 8443 的服务 | 是 | 通过 |
| 切换后监听 8443 的服务 | 是 | 通过 |
| `gost` 8443 回滚路径 | 是 | 通过 |
| 新增、变更监听端口时同步检查云安全组 / 云防火墙 / 服务器防火墙 | 是 | 通过 |

本阶段不执行任何端口、防火墙或远程检查命令。

## Phase 0 到 Phase 7 复核清单

| Phase | 内容 | 是否在草案中明确 | 复核结论 |
| --- | --- | --- | --- |
| Phase 0 | 人工 Go 确认 | 是 | 通过 |
| Phase 1 | 只读状态确认 | 是 | 通过 |
| Phase 2 | 旧链路和 `node.share_link` 备份确认 | 是 | 通过 |
| Phase 3 | 执行窗口确认 | 是 | 通过 |
| Phase 4 | 正式切换步骤草案 | 是 | 通过 |
| Phase 5 | 客户端验收 | 是 | 通过 |
| Phase 6 | 观察期 | 是 | 通过 |
| Phase 7 | 完成记录或回滚 | 是 | 通过 |

## 验证范围复核清单

| 验证项 | 是否在草案中明确 | 复核结论 |
| --- | --- | --- |
| Shadowrocket | 是 | 通过 |
| v2rayN | 是 | 通过 |
| 软路由 | 是 | 通过 |
| 普通网页访问 | 是 | 通过 |
| 目标平台访问 | 是 | 通过 |
| 直播影响观察 | 是 | 通过 |
| 回退链路验证 | 是 | 通过 |
| 敏感信息检查 | 是 | 通过 |
| 不记录完整节点链接 | 是 | 通过 |

## 回滚复核清单

| 回滚项 | 是否在草案中明确 | 复核结论 |
| --- | --- | --- |
| `node.share_link` 异常时恢复旧值 | 是 | 通过 |
| `socat` 18443 或 `socat` 接管 8443 异常时回滚到 `gost` 8443 或原正式链路 | 是 | 通过 |
| 远端服务异常时停止切换并恢复原状态 | 是 | 通过 |
| 云安全组 / 云防火墙 / 服务器防火墙导致不可达时立即停止 cutover 并回滚 | 是 | 通过 |
| 回滚后必须重新验证客户端可用性 | 是 | 通过 |
| 回滚记录不得包含敏感信息 | 是 | 通过 |

## 失败判定复核清单

| 失败判定项 | 是否在草案中明确 | 复核结论 |
| --- | --- | --- |
| 客户端无法连接 | 是 | 通过 |
| Shadowrocket / v2rayN / 软路由任一关键场景失败 | 是 | 通过 |
| 普通网页访问失败 | 是 | 通过 |
| 目标平台访问异常 | 是 | 通过 |
| 直播出现明显影响 | 是 | 通过 |
| 8443 不可达 | 是 | 通过 |
| 回退链路不可用 | 是 | 通过 |
| 出现任何敏感信息写入风险 | 是 | 通过 |

## 停止条件复核清单

| 停止条件 | 是否在草案中明确 | 复核结论 |
| --- | --- | --- |
| 未获得人工 Go | 是 | 通过 |
| 执行窗口未确认 | 是 | 通过 |
| 执行人 / 验收人 / 回滚负责人未确认 | 是 | 通过 |
| `node.share_link` 旧值未备份 | 是 | 通过 |
| 远程命令未授权 | 是 | 通过 |
| 云服务器安全组 / 云防火墙 / 服务器防火墙未确认 | 是 | 通过 |
| `gost` 8443 回退链路不可用 | 是 | 通过 |
| 客户端无法快速回退 | 是 | 通过 |
| 发现真实密钥、完整链接、token、服务器密码写入风险 | 是 | 通过 |

## 当前缺口 / 待补充项

当前仍缺少：

- 真实执行级命令白名单。
- 正式执行窗口。
- 执行人、验收人、回滚负责人。
- `node.share_link` 旧值安全备份确认。
- 8443 云安全组 / 云防火墙 / 服务器防火墙实际检查结果。
- 正式 Go。
- C 方案执行版 runbook 审批。

以上缺口解除前必须保持 No-Go。

## 阶段结论

- Stage 3.3.17 草案结构可复核。
- Stage 3.3.18 只产出 review checklist。
- 本阶段完成后仍然保持 No-Go。
- 不得将本阶段解释为正式 cutover 授权。
- 本阶段不执行任何正式切换、远程命令、后端任务或数据库修改。

## 安全边界声明

- 本阶段只写文档复核清单。
- 本阶段不修改代码。
- 本阶段不修改 `.env`。
- 本阶段不读取或修改私钥、服务器密码、真实数据库、真实日志。
- 本阶段不写入真实 SSH Key。
- 本阶段不写入 Passphrase。
- 本阶段不写入 token。
- 本阶段不写入完整节点链接。
- 本阶段不修改 `node.share_link`。
- 本阶段不新增数据库迁移。
- 本阶段不新增监听端口。
- 本阶段不执行远程命令。
- 本阶段不触发后端任务。
- 本阶段不关闭、停用或降级 `gost` 8443。
- 本阶段不让 `socat` 接管 `8443`。
- 本阶段不做正式 cutover。
