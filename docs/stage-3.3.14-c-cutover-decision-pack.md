# Stage 3.3.14 C Cutover Decision Pack

本阶段只整理 C 方案正式 cutover 前的最终决策包 / 预审文档。本阶段不是正式 cutover，不连接服务器，不执行远程命令，不触发后端任务，不修改业务代码，不修改正式节点链接。

## 当前阶段结论

- 本阶段只是 C 方案决策包 / 预审文档。
- 当前暂不进入 C 方案正式执行。
- 当前不批准正式 cutover。
- `node.share_link` 不允许修改。
- 不允许执行远程命令。
- 不允许 SSH 登录。
- 不允许 systemd 操作。
- 不允许防火墙或端口切换操作。
- 不允许 `socat` 接管 8443。
- `socat` 18443 继续作为候选链路。
- 不允许关闭、停用或降级 `gost` 8443。
- `gost` 8443 继续保留为当前正式 / 回退链路。
- 当前没有正式 cutover 执行窗口。
- 如未来进入 C 方案并出现异常，应立即回滚到 `gost` 8443 或原正式链路；本阶段仅记录该回滚原则。

## B+ readiness 结果摘要

Stage 3.3.13 已记录 B+ readiness 状态：

- 最终方案：B+。
- 是否允许正式 cutover：否。
- 是否允许修改 `node.share_link`：否。
- 是否允许远程命令：否。
- 是否让 `socat` 接管 8443：否。
- 是否停用 `gost` 8443：否。
- `socat` 18443 客户端已真实测试过。
- 测试时间：2026-06-07。
- Shadowrocket 场景：可用。
- v2rayN 场景：可用。
- 软路由场景：可用。
- 是否影响直播：无影响。
- `gost` 8443 继续保留为正式 / 回退链路。

## C 方案准入条件表

| 准入条件 | 当前状态 | 是否满足 | 说明 |
| --- | --- | --- | --- |
| 正式 cutover 获得人工 Go | 未批准 | 否 | 当前仅允许决策包 / 预审文档 |
| 允许修改 `node.share_link` | 未批准 | 否 | 当前不允许修改 |
| 允许远程命令 | 未批准 | 否 | 当前不允许 SSH、systemd、防火墙或端口切换 |
| 允许 `socat` 接管 8443 | 未批准 | 否 | `socat` 18443 继续作为候选链路 |
| 允许停用、降级或替换 `gost` 8443 | 未批准 | 否 | `gost` 8443 继续保留 |
| 正式执行窗口 | 未确定 | 否 | 未来需选择非直播时段、低风险窗口 |
| 回滚方案 | 原则已记录 | 部分 | 未来执行前仍需操作级 runbook |
| 客户端测试 | 已通过三类场景 | 是 | Shadowrocket、v2rayN、软路由均可用 |
| 8443 云安全组 / 云防火墙检查 | 未执行 | 否 | 涉及 8443 切换前必须确认 |
| 8443 服务器防火墙检查 | 未执行 | 否 | 涉及 8443 切换前必须确认 |

结论：当前不满足 C 方案正式 cutover 准入条件，必须保持 No-Go。

## 人工决策结论表

| 决策项 | 人工结论 | 当前记录 |
| --- | --- | --- |
| 是否准备进入 C 方案 | 暂不进入 C 方案正式执行 | 仅允许进入 C 方案决策包 / 预审文档阶段 |
| 如果进入 C，是否允许正式 cutover | 否 | 当前不批准正式 cutover |
| 是否允许修改 `node.share_link` | 否 | 当前不允许修改 |
| 是否允许远程命令 | 否 | 不允许 SSH 登录、systemd 操作、防火墙或端口切换操作 |
| 是否允许 `socat` 接管 8443 | 否 | `socat` 18443 继续作为候选链路 |
| 是否允许停用、降级或替换 `gost` 8443 | 否 | `gost` 8443 继续保留为当前正式 / 回退链路 |
| 正式执行窗口 | 暂不确定 | 当前没有正式 cutover 执行窗口 |
| 出问题时是否立即回滚到 `gost` 8443 | 是 | 未来若进入 C 并异常，应立即回滚；本阶段只记录原则 |

## 是否允许正式 cutover

当前结论：不允许。

确认项：

| 确认项 | 状态 | 说明 |
| --- | --- | --- |
| 人工 Go 是否已给出 | No-Go | 未批准正式 cutover |
| C 方案执行 runbook 是否可执行 | 未完成 | 当前只有预审文档 |
| 执行窗口是否已确认 | 未确认 | 当前没有执行窗口 |
| 回滚负责人是否已确认 | 未确认 | 本阶段只记录原则 |
| 远程操作权限是否已批准 | 未批准 | 不允许远程命令 |

未获得新的人工 Go 前，不得执行正式 cutover。

## 是否允许修改 node.share_link

当前结论：不允许。

确认项：

| 确认项 | 状态 | 说明 |
| --- | --- | --- |
| 是否允许修改 `node.share_link` | 否 | 正式节点链接保持不变 |
| 旧值是否已备份 | 不适用 | 本阶段不修改 |
| 回滚方式是否已执行级确认 | 未确认 | C 方案前需另行确认 |
| 是否允许写入完整链接到文档 | 否 | 禁止写入完整节点链接 |

## 是否允许远程命令

当前结论：不允许。

确认项：

| 确认项 | 状态 | 说明 |
| --- | --- | --- |
| SSH 登录 | 不允许 | 本阶段禁止 |
| systemd 操作 | 不允许 | 本阶段禁止 |
| 防火墙操作 | 不允许 | 本阶段禁止 |
| 端口切换操作 | 不允许 | 本阶段禁止 |
| 远程检查命令 | 不允许 | 本阶段禁止 |
| 后端任务触发 | 不允许 | 本阶段禁止 |

## 是否允许 socat 接管 8443

当前结论：不允许。

确认项：

| 确认项 | 状态 | 说明 |
| --- | --- | --- |
| `socat` 是否允许接管 8443 | 否 | `socat` 18443 继续作为候选链路 |
| 8443 当前监听进程是否确认 | 未确认 | C 方案前必须只读确认 |
| 切换后监听服务是否确认 | 未确认 | C 方案前必须确认 |
| 云安全组 / 云防火墙 TCP 8443 是否放行 | 未确认 | C 方案前必须确认 |
| 服务器防火墙 TCP 8443 是否放行 | 未确认 | C 方案前必须确认 |

## 是否允许停用、降级或替换 gost 8443

当前结论：不允许。

确认项：

| 确认项 | 状态 | 说明 |
| --- | --- | --- |
| 是否允许停用 `gost` 8443 | 否 | 继续保留 |
| 是否允许降级 `gost` 8443 | 否 | 继续保留 |
| 是否允许替换 `gost` 8443 | 否 | 继续保留 |
| `gost` 8443 是否作为回退链路 | 是 | 当前正式 / 回退链路 |

## 正式执行窗口确认表

| 确认项 | 当前状态 | 说明 |
| --- | --- | --- |
| 正式执行窗口 | 暂不确定 | 当前没有正式 cutover 执行窗口 |
| 是否避开直播时段 | 待未来确认 | 如未来批准 C，需选择非直播时段 |
| 是否为低风险窗口 | 待未来确认 | 如未来批准 C，需另行确认 |
| 观察时间 | 未确定 | C 方案前需确认 |
| 客户端回退准备 | 原则已记录 | C 方案前需确认执行细节 |

未来如批准 C 方案，必须另行选择非直播时段、低风险窗口，并重新审批。

## 回滚负责人、执行人、验收人确认表

| 角色 | 当前状态 | 姓名 / 账号 | 说明 |
| --- | --- | --- | --- |
| 执行人 | 未确认 | 待填写 | 当前不执行 |
| 验收人 | 未确认 | 待填写 | 当前不执行 |
| 回滚负责人 | 未确认 | 待填写 | 当前只记录回滚原则 |
| 决策审批人 | 未确认新的 Go | 待填写 | 未获 Go 前保持 No-Go |

## 回滚方案复核表

| 回滚场景 | 回滚原则 | 当前状态 |
| --- | --- | --- |
| `socat` 18443 候选链路异常 | 使用 `gost` 8443 或原正式链路 | 原则已记录 |
| `node.share_link` 修改后异常 | 恢复旧值 | 当前不允许修改 |
| `socat` 接管 8443 后异常 | 恢复 `gost` 8443 | 当前不允许接管 |
| 远端服务异常 | 停止切换并恢复原状态 | 当前不允许远程操作 |
| 云安全组或防火墙异常 | 停止 cutover 并恢复回退链路 | C 方案前需检查 |

回滚记录不得包含完整节点链接、SSH Key、Passphrase、token、服务器密码或 Reality privateKey。

## 8443 端口相关检查

如未来涉及 8443 服务切换，必须确认：

| 检查项 | 当前状态 | C 方案前要求 |
| --- | --- | --- |
| 云服务器安全组 TCP 8443 | 未确认 | 必须确认放行 |
| 云防火墙 TCP 8443 | 未确认 | 必须确认放行 |
| 服务器防火墙 TCP 8443 | 未确认 | 必须检查放行 |
| 当前监听 8443 的进程 | 未确认 | 必须确认 |
| 切换后监听 8443 的服务 | 未确认 | 必须确认 |
| `gost` 8443 回滚方式 | 原则已记录 | 必须形成执行级回滚步骤 |

本阶段不执行任何 8443 检查命令，只记录未来 C 方案的准入要求。

## Go / No-Go checklist

| 检查项 | 当前结论 | Go / No-Go |
| --- | --- | --- |
| B+ readiness 已记录 | 已完成 | Go for documentation only |
| 客户端三类场景测试可用 | 已记录 | Go for documentation only |
| 直播无影响 | 已记录 | Go for documentation only |
| 正式 cutover 是否批准 | 未批准 | No-Go |
| 修改 `node.share_link` 是否批准 | 未批准 | No-Go |
| 远程命令是否批准 | 未批准 | No-Go |
| `socat` 接管 8443 是否批准 | 未批准 | No-Go |
| 停用、降级或替换 `gost` 8443 是否批准 | 未批准 | No-Go |
| 执行窗口是否确认 | 未确认 | No-Go |
| 8443 安全组 / 防火墙是否确认 | 未确认 | No-Go |
| 执行人、验收人、回滚负责人是否确认 | 未确认 | No-Go |

最终结论：No-Go。未获得新的人工 Go 之前必须保持 No-Go。

## 当前剩余阻塞项列表

- 正式 cutover 未获批准。
- 修改 `node.share_link` 未获批准。
- 远程命令执行未获批准。
- `socat` 接管 8443 未获批准。
- 停用、降级或替换 `gost` 8443 未获批准。
- 正式执行窗口尚未确定。
- 执行人、验收人、回滚负责人尚未确认。
- 8443 云服务器安全组 / 云防火墙 / 服务器防火墙尚未确认。
- C 方案执行级回滚步骤尚未单独验收。
- Docker compose 完整部署验收此前仍有本地环境限制记录。

## 未获得新的人工 Go 前的硬性禁止

未获得新的人工 Go 前：

- 不得执行正式 cutover。
- 不得修改 `node.share_link`。
- 不得执行远程命令。
- 不得触发后端任务。
- 不得新增数据库迁移。
- 不得新增监听端口。
- 不得关闭、停用或降级 `gost` 8443。
- 不得让 `socat` 接管 `8443`。
- 不得写入真实 SSH Key、Passphrase、token、服务器密码或完整节点链接。

## 安全边界声明

- 本阶段只写文档记录。
- 本阶段不修改代码。
- 本阶段不修改 `.env`。
- 本阶段不读取或修改私钥、服务器密码、真实数据库、真实日志。
- 本阶段不写入真实 SSH Key。
- 本阶段不写入 Passphrase。
- 本阶段不写入 token。
- 本阶段不写入完整节点链接。
- 本阶段不修改 `node.share_link`。
- 本阶段不新增数据库迁移。
- 本阶段不新增监听端口。
- 本阶段不执行远程命令。
- 本阶段不触发后端任务。
- 本阶段不关闭、停用或降级 `gost` 8443。
- 本阶段不让 `socat` 接管 `8443`。
- 本阶段不做正式 cutover。
