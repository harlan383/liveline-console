# Stage 3.3.34 C Stability Archive

本阶段归档 C-minimal cutover 完成后的稳定状态。本阶段不是新的 cutover，不修改 `node.share_link`，不新增监听端口，不修改防火墙规则，不让 `socat` 接管 8443，不关闭、停用、降级或替换 `gost` 8443。

## 当前阶段结论

- 本阶段是 C-minimal cutover 稳定状态归档阶段。
- 本阶段不是新的 cutover。
- 本阶段不修改 `node.share_link`。
- 本阶段不修改端口。
- 本阶段不修改防火墙。
- 本阶段不关闭 `gost` 8443。
- 本阶段不让 `socat` 接管 8443。
- 本阶段不新增数据库迁移。
- 本阶段不触发回滚。

## 稳定状态汇总

| 项目 | 状态 |
| --- | --- |
| C-minimal cutover | 已完成 |
| Post-cutover observation | Healthy |
| 正式链路 | `socat` 18443 |
| 回退链路 | `gost` 8443 |
| `node.share_link` | 已指向 `socat` 18443 |
| 回滚状态 | 未触发回滚 |
| 客户端观察 | 正常 |
| 直播影响观察 | 无明显影响 |
| 新增监听端口 | 未新增 |
| 数据库迁移 | 未新增 |
| 防火墙规则变更 | 未修改 |

## 链路角色归档

| 链路 | 当前角色 | 归档结论 |
| --- | --- | --- |
| `socat` 18443 | 当前正式链路 | 保持 |
| `gost` 8443 | 当前回退链路 | 保留 |
| 8443 | 仍由 `gost` 使用 | 不由 `socat` 接管 |
| `gost` 8443 | 回退链路 | 不关闭、不降级、不替换 |
| 18443 | 已存在并已验收端口 | 不作为新增端口处理 |

## 后续维护边界

- 不建议立即清理 `gost` 8443。
- 不建议让 `socat` 接管 8443。
- 不建议关闭或替换 `gost` 8443。
- 如未来要清理回退链路，必须单独进入审批阶段。
- 如未来要改端口，必须重新确认云服务器安全组 / 云防火墙 / 服务器防火墙。
- 如未来要再次修改 `node.share_link`，必须重新备份旧值并单独审批。

## 回滚保留策略

- 保留 `gost` 8443 作为回退链路。
- 保留旧 `node.share_link` 备份位置说明，但不得写入完整链接。
- 回滚操作不得写入完整节点链接、SSH Key、Passphrase、token、服务器密码或 Reality privateKey。
- 如果后续出现异常，应优先恢复旧 `node.share_link`，并验证客户端可用性。

## 稳定性归档结论

- 当前无需回滚。
- 当前无需新增阶段性修复。
- 当前建议进入维护观察。
- 当前项目进入 C-minimal cutover 稳定归档状态。

## 当前禁止事项

- 不再次修改 `node.share_link`。
- 不让 `socat` 接管 8443。
- 不关闭、停用、降级或替换 `gost` 8443。
- 不新增监听端口。
- 不新增数据库迁移。
- 不修改防火墙规则。
- 不执行 systemctl start / stop / restart / enable / disable。
- 不写入完整节点链接。

## 安全边界声明

- 本阶段不写入真实 SSH Key。
- 本阶段不写入 passphrase。
- 本阶段不写入 token。
- 本阶段不写入完整节点链接。
- 本阶段不读取或修改 `node.share_link`。
- 本阶段不修改数据库。
- 本阶段不新增数据库迁移。
- 本阶段不新增监听端口。
- 本阶段不修改防火墙。
- 本阶段不关闭 `gost` 8443。
- 本阶段不让 `socat` 接管 8443。
- 本阶段不执行新的 cutover。
