# Stage 3.3.19 C Execution Runbook Gap Fix Plan

本阶段只整理 Stage 3.3.18 复核后发现的 C 方案执行级 runbook 缺口修复计划。本阶段不是正式 cutover，不连接服务器，不执行远程命令，不触发后端任务，不修改业务代码，不修改正式节点链接。

## 当前阶段结论

- 本阶段是 C execution runbook gap fix plan。
- 本阶段不是正式 cutover。
- 当前仍保持 No-Go。
- 本阶段只整理缺口修复计划，不执行任何修复动作。
- 本阶段不修改 `node.share_link`。
- 本阶段不新增数据库迁移。
- 本阶段不新增监听端口。
- 本阶段不执行远程命令。
- 本阶段不触发后端任务。
- 本阶段不关闭、停用或降级 `gost` 8443。
- 本阶段不让 `socat` 接管 `8443`。

## Stage 3.3.18 复核后缺口摘要

Stage 3.3.18 复核后，当前仍缺少：

- 真实执行级命令白名单。
- 正式执行窗口。
- 执行人、验收人、回滚负责人。
- `node.share_link` 旧值安全备份确认。
- 8443 云安全组 / 云防火墙 / 服务器防火墙实际检查结果。
- 正式 Go。
- C 方案执行版 runbook 审批。

以上缺口解除前，C 方案必须继续保持 No-Go。

## Gap Fix 计划总表

| 缺口项 | 当前状态 | 修复目标 | 修复方式 | 负责人 | 是否需要人工审批 | 是否涉及远程命令 | 是否涉及 `node.share_link` | 是否涉及 8443 | 是否涉及 `gost` 8443 | 是否涉及 `socat` | 当前阶段是否执行 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 真实执行级命令白名单 | 未形成 | 形成只读命令、执行命令、禁止命令清单 | 未来单独文档评审 | 待确认 | 是 | 是 | 可能 | 可能 | 可能 | 可能 | 否 | 本阶段不写真实命令 |
| 正式执行窗口 | 未确定 | 确认低风险、非直播时段窗口 | 人工审批 | 待确认 | 是 | 否 | 否 | 可能 | 可能 | 可能 | 否 | 需包含观察与回滚截止时间 |
| 执行人 / 验收人 / 回滚负责人 | 未确认 | 明确角色、职责、在线要求 | 人工审批 | 待确认 | 是 | 可能 | 可能 | 可能 | 可能 | 可能 | 否 | 不记录个人敏感信息 |
| `node.share_link` 旧值备份 | 未确认 | 确认安全备份与恢复方式 | 未来审批与执行级 runbook | 待确认 | 是 | 否 | 是 | 否 | 否 | 否 | 否 | 禁止写入完整链接 |
| 8443 端口实际检查 | 未执行 | 确认云安全组、云防火墙、服务器防火墙与监听服务 | 未来只读检查审批 | 待确认 | 是 | 是 | 否 | 是 | 是 | 可能 | 否 | 本阶段不执行检查 |
| 正式 Go | 未获得 | 形成明确 Go / No-Go 结论 | 人工审批 | 待确认 | 是 | 可能 | 可能 | 可能 | 可能 | 可能 | 否 | 未 Go 前保持 No-Go |
| C 方案执行版 runbook 审批 | 未完成 | 形成 final runbook 并获批 | 单独 PR 和人工审批 | 待确认 | 是 | 可能 | 可能 | 可能 | 可能 | 可能 | 否 | 草案和复核清单不能替代 final |

## 命令白名单缺口修复计划

- 当前不允许远程命令。
- 未来如需允许，必须先列出：
  - 只读命令白名单。
  - 执行命令白名单。
  - 禁止命令清单。
- 必须明确 SSH 登录是否允许。
- 必须明确 systemd 操作是否允许。
- 必须明确端口检查是否允许。
- 必须明确防火墙检查是否允许。
- 必须明确服务切换是否允许。
- 本阶段不写真实命令。
- 本阶段不执行命令。

## 正式执行窗口缺口修复计划

- 当前正式执行窗口未确定。
- 未来必须选择非直播时段和低风险窗口。
- 必须明确开始时间。
- 必须明确结束时间。
- 必须明确观察时间。
- 必须明确回滚截止时间。
- 必须明确超时后的停止条件。
- 本阶段不设置正式执行窗口。

## 负责人缺口修复计划

- 当前执行人、验收人、回滚负责人未最终确认。
- 未来必须确认：
  - 执行人。
  - 验收人。
  - 回滚负责人。
  - 客户端测试负责人。
  - 审批人。
- 每个角色必须明确职责和在线要求。
- 每个角色必须确认在执行窗口内可响应。
- 本阶段不指定真实个人敏感信息。

## `node.share_link` 备份确认缺口修复计划

- 当前不允许修改 `node.share_link`。
- 未来如需修改，必须确认旧值安全备份。
- 未来如需修改，必须确认修改目标。
- 未来如需修改，必须确认恢复旧值方式。
- 未来如需修改，必须确认回滚验证方式。
- 禁止把完整节点链接写入文档、日志、任务结果或报告。
- 本阶段不读取 `node.share_link`。
- 本阶段不写入 `node.share_link`。
- 本阶段不修改 `node.share_link`。

## 8443 端口检查结果缺口修复计划

- 当前不执行任何端口检查命令。
- 未来如涉及 8443 接管，必须确认：
  - 云服务器安全组 TCP 8443 是否放行。
  - 云防火墙 TCP 8443 是否放行。
  - 服务器防火墙 TCP 8443 是否放行。
  - 当前监听 8443 的服务。
  - 切换后监听 8443 的服务。
  - `gost` 8443 回滚路径。
- 凡涉及新增、变更监听端口或 8443 接管，必须同步检查云服务器安全组 / 云防火墙 / 服务器防火墙放行对应 TCP 端口。
- 本阶段不新增监听端口。
- 本阶段不执行 8443 检查命令。

## 正式 Go 缺口修复计划

- 当前仍为 No-Go。
- 未来必须重新人工审批。
- 必须明确是否允许正式 cutover。
- 必须明确是否允许修改 `node.share_link`。
- 必须明确是否允许远程命令。
- 必须明确是否允许 `socat` 接管 8443。
- 必须明确是否允许关闭、停用、降级或替换 `gost` 8443。
- 未形成新的人工 Go 前，不得进入正式 cutover。

## C 方案执行版 Runbook 审批缺口修复计划

- 当前只有草案和复核清单。
- 未来需要生成 execution runbook final 版本。
- final 版本必须包含：
  - 执行步骤。
  - 验证步骤。
  - 回滚步骤。
  - 失败判定。
  - 停止条件。
  - 负责人。
  - 执行窗口。
  - 命令授权范围。
- final 版本必须单独 PR。
- final 版本必须经过人工审批。
- 草案、复核清单和本 gap fix plan 均不得被解释为正式执行授权。

## 缺口优先级

| 优先级 | 缺口 |
| --- | --- |
| P0 | 正式 Go、远程命令授权、`node.share_link` 备份、8443 检查、`gost` 8443 回滚 |
| P1 | 执行窗口、负责人、验收模板、执行版 runbook 审批 |
| P2 | 文档归档、PR 清理、复盘 |

## 阶段结论

- 本阶段完成后仍然保持 No-Go。
- 本阶段不得被解释为正式 cutover 授权。
- 本阶段不解除任何阻塞项。
- 本阶段不执行任何修复动作。
- 只有缺口全部修复并获得新的人工 Go，才能进入正式 cutover。

## 安全边界声明

- 本阶段只写文档。
- 本阶段不修改代码。
- 本阶段不修改 `.env`。
- 本阶段不写入真实 SSH Key。
- 本阶段不写入 passphrase。
- 本阶段不写入 token。
- 本阶段不写入完整节点链接。
- 本阶段不读取或修改真实数据库。
- 本阶段不修改 `node.share_link`。
- 本阶段不新增数据库迁移。
- 本阶段不新增监听端口。
- 本阶段不执行远程命令。
- 本阶段不触发后端任务。
- 本阶段不关闭 `gost` 8443。
- 本阶段不让 `socat` 接管 `8443`。
- 本阶段不做正式 cutover。
