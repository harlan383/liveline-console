# Stage 3.3.29 C Final Go Decision

本阶段记录 C 方案最终 Go 决策。本阶段不是正式 cutover 执行，不修改 `node.share_link`，不执行远程命令，不 SSH，不触发后端任务，不新增数据库迁移，不新增监听端口，不让 `socat` 接管 8443，不关闭、停用、降级或替换 `gost` 8443。

## 当前阶段结论

- 本阶段是 C 方案最终 Go 决策阶段。
- 本阶段记录最终 Go 决策。
- 当前最终结论为 `Go`，但仅限进入后续正式 cutover 执行阶段。
- 采用方案为 `C-minimal Go`。
- 本阶段不是正式 cutover 执行。
- 本阶段不修改 `node.share_link`。
- 本阶段不执行远程命令。
- 本阶段不触发后端任务。
- 本阶段不切换端口。

## 最终 Go 决策表

| 决策项 | 结论 | 边界 |
| --- | --- | --- |
| 是否允许进入 C 方案正式 cutover 后续执行阶段 | Go | 仅允许进入后续执行阶段，不代表本阶段执行 |
| 是否允许修改 `node.share_link` | Go | 仅限后续正式执行阶段，必须按 runbook 执行 |
| 是否允许 `socat` 接管 8443 | No-Go | 18443 继续作为候选正式链路 |
| 是否允许关闭 / 停用 / 降级 / 替换 `gost` 8443 | No-Go | `gost` 8443 必须继续保留为正式 / 回退链路 |
| 是否允许新增监听端口 | No-Go | 继续使用已验收的 18443 |
| 是否允许新增数据库迁移 | No-Go | 不新增 Alembic / 表 / 字段 |
| 是否允许执行远程写命令 | No-Go | 后续执行阶段也需单独 runbook 审批 |
| 是否允许触发后端任务 | 待后续正式执行 runbook 单独审批 | 本阶段不触发任何任务 |
| 是否保留 `gost` 8443 回退链路 | 是 | 不关闭、不替换、不降级 |

## C-minimal Go 范围说明

- 使用 `socat` 18443 作为已验收候选链路。
- 不让 `socat` 接管 8443。
- 不关闭 `gost` 8443。
- 不替换 `gost` 8443。
- 不新增端口。
- 仅允许后续正式执行阶段按 runbook 修改 `node.share_link`。
- `node.share_link` 修改必须可回滚。
- 修改 `node.share_link` 前必须备份旧值。
- 修改 `node.share_link` 后必须进行客户端验收。
- 出现异常必须立即恢复旧 `node.share_link`。
- 不允许写入完整节点链接到文档、日志、任务结果或报告。

## 当前已完成 readiness

| Readiness 项 | 状态 | 说明 |
| --- | --- | --- |
| B+ 客户端验收 | Ready | Shadowrocket / v2rayN / 软路由三类场景可用 |
| 服务器侧只读预检 | Ready | 已记录脱敏证据 |
| `gost` 8443 监听 | Ready | 当前正在监听 |
| `socat` 18443 监听 | Ready | 当前正在监听 |
| `gost` systemd | active | 已记录 |
| `socat` systemd | active | 已记录 |
| 服务器侧防火墙 | 未发现本地阻塞 | 已记录 |
| 云服务器安全组 TCP 8443 | 已放行 | 已人工确认 |
| 云服务器安全组 TCP 18443 | 已放行 | 已人工确认 |
| 独立云防火墙 | Not applicable | 当前服务商后台未发现独立云防火墙功能 |
| 云侧安全组 / 云防火墙确认项 | 完成 | Stage 3.3.27 已记录 |

## 执行前强制条件

| 强制条件 | 要求 |
| --- | --- |
| 旧 `node.share_link` 已安全备份 | 必须完成，且不得把完整链接写入文档、日志、任务结果或报告 |
| 新 `node.share_link` 已确认 | 必须完成，且不得写入文档 |
| 客户端快速回退方式已准备 | 必须确认 |
| 执行窗口已确认 | 必须确认 |
| 执行人、验收人、回滚负责人已确认 | 必须确认 |
| 回滚步骤已确认 | 必须确认 |
| 敏感信息脱敏规则已确认 | 必须确认 |

## 回滚原则

- `node.share_link` 异常时立即恢复旧值。
- `socat` 18443 异常时回退到 `gost` 8443 或原正式链路。
- 客户端验收失败时立即回滚。
- 直播出现明显异常时立即回滚。
- 回滚记录不得包含完整节点链接、SSH Key、Passphrase、token、服务器密码或 Reality privateKey。

## 端口和防火墙提醒

- 8443 当前由 `gost` 监听，继续作为正式 / 回退链路。
- 18443 当前由 `socat` 监听，作为候选链路。
- 香港中转机 TCP 8443 / 18443 云安全组已放行。
- 独立云防火墙为 `Not applicable`。
- 服务器防火墙未发现本地阻塞。
- 凡未来涉及新增、变更监听端口或 8443 接管，仍必须重新检查云服务器安全组 / 云防火墙 / 服务器防火墙是否放行对应 TCP 端口。

## 下一步建议

- 进入 Stage 3.3.30-c-formal-cutover-execution-runbook-final。
- Stage 3.3.30 应生成正式执行 runbook final。
- 只有 Stage 3.3.30 合并并人工确认后，才能进入真实正式 cutover 执行。
- 不得跳过执行 runbook final 直接操作生产链路。

## 安全边界声明

- 本阶段不写入真实 SSH Key。
- 本阶段不写入 passphrase。
- 本阶段不写入 token。
- 本阶段不写入完整节点链接。
- 本阶段不读取或修改 `node.share_link`。
- 本阶段不修改数据库。
- 本阶段不新增数据库迁移。
- 本阶段不新增监听端口。
- 本阶段不执行远程命令。
- 本阶段不 SSH。
- 本阶段不触发后端任务。
- 本阶段不关闭 `gost` 8443。
- 本阶段不让 `socat` 接管 8443。
- 本阶段不做正式 cutover。
